services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: fancyit
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: fancyit
    ports: ["5432:5432"]
    volumes: ["dbdata:/var/lib/postgresql/data"]
volumes:
  dbdata: